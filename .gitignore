
# Node
node_modules
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
*-live.log
dist
build
playwright-report
test-results
apps/dashboard/playwright-report
apps/dashboard/test-results

# Env
.env
.env.*
!server/.env.example
apps/**/.env*
!apps/**/.env*.example
server/.env
server/.docker-prisma-bootstrapped

# Editor
.vscode
.idea
.claude

# OS
.DS_Store
Thumbs.db
*.git_acl_backup_*.txt

# Insomnia API client export (generated)
docs/insomnia/

# CI: evo-import-smoke checks the Game repo out here (embedded git repo).
# Must never be staged by `git add -A` (was committed as a 160000 gitlink).
_game/

# Per-machine import history log, written by server/scripts/evo-import.js
# (see docs/process/evo-import.md). Local by design: it records when THIS
# machine's standing DB was last updated.
server/logs/
